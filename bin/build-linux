#! /bin/zsh

# Build files
yarn make --platform="linux" --arch="ia32"
yarn make --platform="linux" --arch="x64"

# Package to debian package
electron-installer-debian --src out/bizzey-linux-x64/ --dest out/make/ --arch amd64