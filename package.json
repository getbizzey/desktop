{
  "scripts": {
    "build": "yarn build:mac && yarn build:linux && yarn build:windows",
    "build:mac": "bin/build-mac",
    "build:linux": "bin/build-linux",
    "build:windows": "bin/build-windows"
  },
  "dependencies": {
    "nativefier": "^45.0.0"
  }
}
